<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.naats.naatkidunya">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <permission android:name="android.permission.MEDIA_CONTENT_CONTROL" />

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.SET_WALLPAPER" />
    <uses-permission android:name="android.permission.DOWNLOAD_WITHOUT_NOTIFICATION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <permission android:name="android.permission.MEDIA_CONTENT_CONTROL" />

    <application
        android:name=".SharedPref.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/applogo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/applogo"
        android:supportsRtl="true"
        android:theme="@style/HomeScreen"
        android:requestLegacyExternalStorage="true">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".activities.MapsActivity"
            android:label="@string/title_activity_maps"></activity>
        <activity android:name=".activities.AdminContactActivity" />
        <activity android:name=".activities.IslamicRingtonesActivity" />
        <activity android:name=".activities.SplashActivity" />
        <activity android:name=".activities.WelcomeActivity" />
        <activity android:name=".activities.UserInfoActivity" />
        <activity android:name=".activities.UserFeedbackActivity" />
        <activity android:name=".activities.FavoriteActivity" />
        <activity android:name=".activities.BestNaatActivity" />
        <activity android:name=".activities.AllNaatKhawanActivity" />
        <activity android:name=".activities.HomeScreenActivity" />
        <activity
            android:name=".activities.SplashScreenNaatsActivity"
            android:theme="@style/Theme.Transparent">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.NaatKhawanNaatsActivity" />
        <activity
            android:name=".activities.NaatkiDunyaMediaPlayer"

            android:theme="@style/Theme.UserDialog" /> <!-- Firebase Notifications -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-1359902359290652~8786296051" />


        <receiver android:name=".Services.NotificationActionServices"/>
        <service android:name=".Services.onClearFromRecentServices"/>

    </application>

</manifest>
    <!--            android:configChanges="orientation"-->
    <!--            android:screenOrientation="sensor"-->